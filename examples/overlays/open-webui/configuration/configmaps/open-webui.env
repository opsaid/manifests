# 只写与 base 不同的键；域名、对象存储等环境值仍是虚构示例，不可直接用于生产。
# namespace 与 base 相同，REDIS_URL 直接继承 base，无需覆盖；改名时必须同步 REDIS_URL。
WEBUI_URL=https://open-webui.apps.example.com
OPENID_REDIRECT_URI=https://open-webui.apps.example.com/oauth/oidc/callback
S3_ENDPOINT_URL=https://s3.example.com
S3_REGION_NAME=us-east-1
S3_BUCKET_NAME=open-webui-example
