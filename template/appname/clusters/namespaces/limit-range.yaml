apiVersion: v1
kind: LimitRange
metadata:
  name: limit-range
  namespace: appname
spec:
  limits:
  - default:
      cpu: 1
      memory: 1Gi
    defaultRequest:
      cpu: 200m
      memory: 256Mi
    type: Container
